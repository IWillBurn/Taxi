app:
  debug: true
  shutdown_timeout: 20s
database:
  dsn: postgres://
  migrations_dir: "file://../../migrations/trip_service"
connection:
  inbound :
    topic : "trip_inbound"
    brokers : ["kafka:29092"]
  outbound :
     topic : "trip_outbound"
     brokers :  ["kafka:29092"]